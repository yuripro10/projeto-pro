using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class teste8 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int i = 1;
        while (i < 6)
        {
            Debug.Log("O valor do i é: " + i);
            i++;
        }
    }


    // Update is called once per frame
    void Update()
    {
        
    }
}
