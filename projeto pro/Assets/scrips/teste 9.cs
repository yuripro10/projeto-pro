using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class teste9 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int count = 1;
        do
        {
            Debug.Log(count);//Ação
            count++;
        } while (count <= 10); //verificação
        


    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
